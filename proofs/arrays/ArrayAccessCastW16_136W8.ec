from Jasmin require import JWord JWord_array.

require import ArrayWords136W8.

clone export ArrayAccessCast as ArrayAccessCastW16_136W8  with op sizeWS <- 2,
                                                               op sizeWB <- 1,
                                                               op sizeB <- 136,
                                                               theory WordS <- W16,
                                                               theory WordB <- W8,
                                                               theory ArrayWordsB <= ArrayWords136W8.
