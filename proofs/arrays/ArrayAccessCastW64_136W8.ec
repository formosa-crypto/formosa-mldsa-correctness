from Jasmin require import JWord JWord_array.

require import ArrayWords136W8.

clone export ArrayAccessCast as ArrayAccessCastW64_136W8  with op sizeWS <- 8,
                                                               op sizeWB <- 1,
                                                               op sizeB <- 136,
                                                               theory WordS <- W64,
                                                               theory WordB <- W8,
                                                               theory ArrayWordsB <= ArrayWords136W8.
