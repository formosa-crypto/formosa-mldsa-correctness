from Jasmin require import JWord JWord_array.

require import Array2 WArray2.

clone export ArrayWords as ArrayWords2W8  with op sizeW <- 1, op sizeA <- 2,
                                               theory Word <= W8,
                                               theory ArrayN <= Array2,
                                               theory WArrayN <= WArray2.
