from Jasmin require import JWord JWord_array.

require import Array66 WArray66.

clone export ArrayWords as ArrayWords66W8  with op sizeW <- 1,
                                                op sizeA <- 66,
                                                theory Word <= W8,
                                                theory ArrayN <= Array66,
                                                theory WArrayN <= WArray66.
