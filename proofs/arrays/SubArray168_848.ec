from Jasmin require import JArray.

require import Array168 Array848.

clone export SubArray as SubArray168_848  with op sizeS <- 168,
                                               op sizeB <- 848,
                                               theory ArrayS <= Array168,
                                               theory ArrayB <= Array848.
