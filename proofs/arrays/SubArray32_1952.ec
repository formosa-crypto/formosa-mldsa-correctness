from Jasmin require import JArray.

require import Array32 Array1952.

clone export SubArray as SubArray32_1952  with op sizeS <- 32,
                                               op sizeB <- 1952,
                                               theory ArrayS <= Array32,
                                               theory ArrayB <= Array1952.
