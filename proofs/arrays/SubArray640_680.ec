from Jasmin require import JArray.

require import Array640 Array680.

clone export SubArray as SubArray640_680  with op sizeS <- 640,
                                               op sizeB <- 680,
                                               theory ArrayS <= Array640,
                                               theory ArrayB <= Array680.
